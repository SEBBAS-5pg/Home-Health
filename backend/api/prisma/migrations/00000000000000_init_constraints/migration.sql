-- Esta migración quedó deprecada. Los CHECK constraints ahora se aplican
-- desde prisma/seed.ts con bloques DO idempotentes. La carpeta se mantiene
-- vacía para no romper referencias en docs/ADR.md §012.
SELECT 1;
