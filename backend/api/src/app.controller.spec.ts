// Test del scaffold reemplazado. Los nuevos tests viven junto a cada servicio.
describe('placeholder', () => {
  it('reemplazado por tests de dominio', () => {
    expect(true).toBe(true);
  });
});
